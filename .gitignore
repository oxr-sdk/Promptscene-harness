# Unity project — has its own git repo (oxr-sdk/XRCollabDemo)
/XRCollabDemo/

# Unity project — has its own git repo (oxr-sdk/XumFlow, runtime branch)
/XumFlow/

# Unity project — same repo, studio branch (content-authoring / port target)
/XumFlow-studio/

# Huge import log (161MB)
/xrcollab_import.log

# Scratch / temp
/scratch/
/tmp_*.txt

# oxr-docs-routing 0층 심볼 인덱스 — 생성물(build-index.sh 로 각자 로컬 생성).
# 커밋 금지: oxr-sdk 패키지는 private 인데 이 레포(Promptscene-harness)는 PUBLIC 이라
# 시그니처 원문을 담은 인덱스를 커밋하면 API 표면 유출이 된다. 생성기 스크립트만 커밋한다.
/promptscene/.index/

# Vendored Node.js toolchain under docs (regeneratable, large)
/docs/.tools/

# OS / editor noise
.DS_Store
Thumbs.db
