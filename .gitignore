# Unity project — has its own git repo (oxr-sdk/XRCollabDemo)
/XRCollabDemo/

# Unity project — has its own git repo (oxr-sdk/XumFlow, runtime branch)
/XumFlow/

# Unity project — same repo, studio branch (content-authoring / port target)
/XumFlow-studio/

# Huge import log (161MB)
/xrcollab_import.log

# Scratch / temp
/scratch/
/tmp_*.txt

# Vendored Node.js toolchain under docs (regeneratable, large)
/docs/.tools/

# OS / editor noise
.DS_Store
Thumbs.db
